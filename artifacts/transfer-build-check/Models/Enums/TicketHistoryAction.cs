namespace EquipmentManagementBackend.Models.Enums;

public enum TicketHistoryAction
{
    created,
    status_changed,
    assigned,
    reassigned,
    approved,
    rejected,
    completed,
    comment_added
}
