namespace EquipmentManagementBackend.Models.Enums;

public enum TicketStatus
{
    open,
    in_progress,
    pending_approval,
    approved,
    rejected,
    completed,
    overdue,
    cancelled
}
